apiVersion: v1
kind: Service
metadata:
  name: {{ include "scibu.fullname" . }}
  labels:
    {{- include "scibu.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scibu.selectorLabels" . | nindent 4 }}
